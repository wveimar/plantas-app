import {getPlantasById} from "../selectors/getPlantasById"
export const ResultadoInsumos = ({publisher}) => {
    const plantas = getPlantasById(publisher);
    return (
        <div>
            
        </div>
    )
}
